### Problem of the Day:
Write a function that takes a string as input and reverse only the vowels of a string.

### Example:

```shell script
  Input: "hello"
  Output: "holle"

  Input: "leetcode"
  Output: "leotcede"
```